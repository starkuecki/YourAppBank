services:
  postgres:
    image: postgres:17
    container_name: bankdata-postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: bankdata
    ports:
      - "5432:5432"
    volumes:
      - bankdata-pgdata:/var/lib/postgresql/data

volumes:
  bankdata-pgdata: