// src/store.js

class BankStore {
// .. globale State-Variablen hier definieren
}

// Wir exportieren wieder eine EINZIGE Instanz der Klasse (Singleton)
const store = new BankStore();
export default store;